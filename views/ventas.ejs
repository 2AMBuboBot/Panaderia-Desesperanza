<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Reporte de Ventas</title>
  <link rel="icon" type="image/png" href="/img/logo.png?v=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <link rel="stylesheet" href="style.css">
  <style>
    /* AJUSTES ESPECÍFICOS DE ESTA PÁGINA */
    .contenedor-ventas {
      max-width: 1100px;
      margin: 40px auto;
      padding: 0 20px;
      text-align: center;
    }

    h1 {
      color: black;
      font-size: 2rem;
      margin-bottom: 30px;
      font-weight: 600;
      border-bottom: 3px solid black;
      display: inline-block;
      padding-bottom: 6px;
    }

    #graficaVentas {
      background: #fff;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.15);
      margin-top: 20px;
    }

  </style>
</head>
<body>

<header class="navbar navbar-expand-lg navbar-light shadow-sm px-4 py-2" 
        style="background: linear-gradient(90deg, #b9935a, #a4773a);">

  <div class="container-fluid">

    <!-- IZQUIERDA: Logo -->
    <a class="navbar-brand d-flex align-items-center" href="/index.html">
      <img src="img/logo.png"
           alt="Logo" width="35" class="me-2" style="border-radius:50%;">
      <span class="fw-bold" style="color: black;">
  Panadería <span class="text-warning">La Desesperanza</span>
</span>
    </a>

    <!-- TOGGLE RESPONSIVE -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menuPrincipal">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- MENÚ -->
    <div class="collapse navbar-collapse" id="menuPrincipal">

      <!-- CENTRO: Navegación -->
      <ul class="nav mb-2 mb-md-0 justify-content-center flex-grow-1">
        <li><a href="/index.html" class="nav-link px-2 text-white">Tienda</a></li>
        <li><a href="/sobre" class="nav-link px-2 text-white">Sobre Nosotros</a></li>

        <!-- SOLO ADMIN -->
        <li id="menuVentas" style="display:none;">
          <a href="/ventas" class="nav-link px-2 text-warning fw-semibold">Ventas</a>
        </li>
      </ul>

      <!-- DERECHA: Carrito + Logout -->
      <div class="d-flex align-items-center gap-3">
        <a href="carrito.html" class="btn btn-outline-dark">
          🛒 Carrito
        </a>

        <button id="btnLogout" class="btn btn-danger">
          Cerrar sesión
        </button>

        <a href="/perfil.html" class="avatar-link" title="Mi perfil">
    <div class="avatar" style="width:36px;height:36px;">
      <img src="https://cdn-icons-png.freepik.com/512/12225/12225881.png" alt="perfil">
    </div>
  </a>
      </div>

    </div>
  </div>
</header>

<main class="contenedor">

  <h1 class="text-center mt-4 titulo-seccion">Reporte de Ventas</h1>
  <canvas id="graficaVentas" width="400" height="200" class="mx-auto d-block mt-4"></canvas>

</main>

<footer>
  <p>© 2025 Panadería “La Desesperanza” — Hecho con ❤️ y harina</p>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
// Mostrar botón ventas solo si es admin
fetch("/api/session")
  .then(r => r.json())
  .then(data => {
    if (data.tipo === "admin") document.getElementById("menuVentas").style.display = "block";
  });

// Cerrar sesión
document.getElementById("btnLogout").onclick = () => {
  fetch("/api/logout").then(() => location.href = "/login.html");
};

// Cargar datos de la gráfica
async function cargarDatos() {
  const resp = await fetch("/api/ventas");
  const data = await resp.json();

  const ctx = document.getElementById("graficaVentas");
  new Chart(ctx, {
    type: "bar",
    data: {
      labels: data.productos,
      datasets: [{
        label: "Productos vendidos",
        data: data.cantidades,
        backgroundColor: "rgba(186, 135, 68, 0.8)"
      }]
    }
  });
}
cargarDatos();
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
