<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sobre Nosotros — Panadería La Desesperanza</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/img/logo.png?v=1">
  <link rel="stylesheet" href="style.css">

  <style>
    .hero-img {
      width: 100%;
      height: 350px;
      object-fit: cover;
      border-radius: 15px;
    }

    .section-title {
      font-weight: bold;
      font-size: 2rem;
      margin-bottom: 10px;
      border-left: 5px solid #f39c12;
      padding-left: 10px;
    }

    .map-container {
      width: 100%;
      height: 400px;
      border-radius: 15px;
      overflow: hidden;
    }
  </style>

  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

</head>

<script>
fetch("/api/session", { credentials: "include" })
  .then(r => r.json())
  .then(data => {
    if (data.loggedIn && data.tipo === "admin") {
      document.getElementById("menuVentas").style.display = "block";
    }
  });
</script>

<body>
  <header class="navbar navbar-expand-lg navbar-light bg-white shadow-sm px-4 py-2">
  <div class="container-fluid">

    <!-- IZQUIERDA -->
    <a class="navbar-brand d-flex align-items-center" href="index.html">
      <img src="img/logo.png"
           alt="Logo" width="35" class="me-2">
      <span class="fw-bold">Panadería <span class="text-warning">La Desesperanza</span></span>
    </a>

    <!-- MENU RESPONSIVE -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menuPrincipal">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="menuPrincipal">

      <!-- CENTRO -->
      <ul class="nav mb-2 mb-md-0 justify-content-center flex-grow-1">
        <li><a href="index.html" class="nav-link px-2 text-white">Tienda</a></li>
        <li><a href="/sobre" class="nav-link px-2 text-white">Sobre Nosotros</a></li>
        <li id="menuVentas" style="display:none;">
          <a href="/ventas" class="nav-link px-2 text-warning fw-semibold">Ventas</a>
        </li>
      </ul>

      <!-- DERECHA -->
      <div class="d-flex align-items-center gap-3">
        <a href="carrito.html" class="btn btn-outline-dark">
          🛒 Carrito
        </a>

        <button id="btnLogout" class="btn btn-danger">
          Cerrar sesión
        </button>

        <a href="/perfil.html" class="avatar-link" title="Mi perfil">
    <div class="avatar" style="width:36px;height:36px;">
      <img src="https://cdn-icons-png.freepik.com/512/12225/12225881.png" alt="perfil">
    </div>
  </a>
      </div>

    </div>
  </div>
</header>

  <main class="container my-5">

   <!-- HERO -->
<div class="text-center mb-4">
  <img src="https://images.pexels.com/photos/2434/bread-food-healthy-breakfast.jpg"
       alt="Panes artesanales" class="hero-img">
</div>

    <!-- HISTORIA -->
    <section class="mb-5">
      <h2 class="section-title">Nuestra Historia</h2>
      <p class="fs-5">
        La Panadería <b>“La Desesperanza”</b> nació en 1995 como un pequeño local familiar
        en el corazón de la <b>Ciudad de México</b>, donde las recetas tradicionales se han
        transmitido de generación en generación.
      </p>
      <p class="fs-5">
        Con el paso de los años seguimos creciendo gracias a nuestros clientes, manteniendo
        el pan artesanal, los hornos tradicionales y la esencia del sabor mexicano.
        Hoy contamos con varias sucursales dentro de CDMX, llevando pan fresco a más familias.
      </p>
    </section>

    <!-- MISIÓN / VISIÓN -->
    <section class="row text-center mb-5">
      <div class="col-md-4 mb-3">
        <div class="p-4 shadow rounded">
          <h4 class="text-warning fw-bold">Misión</h4>
          <p>Ofrecer pan artesanal con calidad excepcional, manteniendo la esencia tradicional mexicana.</p>
        </div>
      </div>
      <div class="col-md-4 mb-3">
        <div class="p-4 shadow rounded">
          <h4 class="text-warning fw-bold">Visión</h4>
          <p>Ser la panadería favorita de las familias, expandiendo nuestra tradición a más ciudades.</p>
        </div>
      </div>
      <div class="col-md-4 mb-3">
        <div class="p-4 shadow rounded">
          <h4 class="text-warning fw-bold">Valores</h4>
          <p>Honestidad, tradición, calidad, respeto y amor por el oficio.</p>
        </div>
      </div>
    </section>

    <!-- LOCALIZACIONES (Leaflet después) -->
    <section class="mb-5">
      <h2 class="section-title">Nuestros Locales</h2>
      <p class="fs-5">Aquí puedes ver dónde se encuentran nuestras panaderías:</p>

      <div id="map" class="map-container"></div>
    </section>

  </main>

  <footer class="text-center py-3 bg-light mt-5">
    © 2025 Panadería “La Desesperanza” — Hecho con ❤️ y harina
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    // Logout igual que en index
    document.getElementById("btnLogout").addEventListener("click", () => {
      fetch("/api/logout", { method: "POST", credentials: "include" })
      .then(r => r.json())
      .then(() => window.location.href = "login.html");
    });
  </script>

  <!-- LEAFLET JS -->
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>

<script>
// Crear el mapa en CDMX
const map = L.map('map').setView([19.4326, -99.1332], 12);

// Cargar los tiles de OpenStreetMap
L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
  attribution: '&copy; OpenStreetMap contributors'
}).addTo(map);

// --- MARCADORES DE CDMX ---

// Sucursal Lindavista
L.marker([19.4897, -99.1251])
  .addTo(map)
  .bindPopup("Sucursal Lindavista — Panadería La Desesperanza");

// Sucursal La Condesa
L.marker([19.4149, -99.1749])
  .addTo(map)
  .bindPopup("Sucursal La Condesa — Panadería La Desesperanza");

// Sucursal La Roma
L.marker([19.4194, -99.1620])
  .addTo(map)
  .bindPopup("Sucursal La Roma — Panadería La Desesperanza");
</script>

</body>
</html>
